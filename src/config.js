window.Game = window.Game || {};
Game.config = {
  gridSize: 40,
  network: {
    enabled: true,
    url: null,
    sendInterval: 0.02,
    authoritative: true,
  },
};

Game.debug = {
  enabled: false,
};
