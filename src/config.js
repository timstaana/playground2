window.Game = window.Game || {};
Game.config = {
  gridSize: 40,
  network: {
    enabled: true,
    url: null,
    sendInterval: 0.016,
    authoritative: false,
  },
};

Game.debug = {
  mode: 0,
  maxMode: 2,
};
